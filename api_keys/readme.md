Create the file .env.api_keys here and add your OPENROUTER API keys to it in the form:

KEY_NAME=key_value

for each name-value pair in a new line. This .env.api_keys file is git-ignored for security reasons.